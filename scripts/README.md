# Scripts

This directory will hold repeatable developer commands for seeding data, running smoke tests, and demo preparation.

Planned scripts:

- `seed_demo_data`
- `run_ingestion`
- `smoke_test`
- `record_demo_checklist`

Current scripts:

- `ingestion_smoke_check.py`: calls the running local API to verify manual ingestion, duplicate handling, and RSS ingestion.
